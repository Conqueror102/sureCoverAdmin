import { AuthCard } from "@/features/auth/auth-card";

export default function ForgotPasswordPage() {
  return <AuthCard mode="forgot" title="Recover access" description="Send a secure password recovery link to your admin email." />;
}
