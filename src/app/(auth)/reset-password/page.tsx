import { AuthCard } from "@/features/auth/auth-card";

export default function ResetPasswordPage() {
  return <AuthCard mode="reset" title="Reset password" description="Create a new password for this protected admin session." />;
}
