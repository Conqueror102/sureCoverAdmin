import { AuthCard } from "@/features/auth/auth-card";

export default function LoginPage() {
  return <AuthCard mode="login" title="Admin login" description="Authenticate with your organization role and MFA policy." />;
}
